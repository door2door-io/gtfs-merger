# Config file for automatic testing at travis-ci.org

sudo: false

language: python

python:
  - "3.6"

install:
  - apt-get update -y
  - make install

script:
  - make test
  - codeclimate-test-reporter --file .coverage
